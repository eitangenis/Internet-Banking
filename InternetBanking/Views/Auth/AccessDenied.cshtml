@using InternetBanking.Core.Application.ViewModels.User

@model string
@{
    ViewData["Title"] = "Access Denied";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-body">
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            
                        <h2 class="alert-heading mb-4">@InternetBanking.WebApp.Resources.Resource.NoPermissionSection</h2>
                       
                        <p>
                            @InternetBanking.WebApp.Resources.Resource.NoPermissionGoBack
                            <a href="@Url.Action("Index", "Home")" class="alert-link">start</a>.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@* <a class="btn btn-warning" asp-controller="Home" asp-action="Index"> *@
